# 内容更新指南（手机就能发）

> 部署上线后，更新内容**不需要电脑**：
> 手机浏览器打开 GitHub → 改一个 JSON 文件 → 点提交 → 约 2 分钟自动上线。

## 三种活内容，三个文件

| 文件 | 内容 | 更新节奏 |
|---|---|---|
| `src/data/content/now.json` | 首页「此刻」条（正在听/做/找） | 每周看一眼 |
| `src/data/content/sampling.json` | 采样笔记（一首歌的祖谱） | 听到好采样随手记 |
| `src/data/content/notes.json` | 手记（长文） | 每月一篇 |

另有 `quotes.json`（首页引语原声）——基本不动，除非有新的用户原话。

## 手机操作步骤（以发一篇手记为例）

1. 浏览器打开仓库：`github.com/kimichen24/kimi-portfolio`
2. 进入 `src/data/content/` → 点 `notes.json`
3. 点右上角**铅笔图标** ✏️ 进入编辑
4. 照抄已有的一篇的格式，改成你的新内容（注意逗号引号别少）
5. 拉到底点 **Commit changes**（绿色按钮）
6. 等 2 分钟，GitHub Actions 自动构建部署，网站已更新

> 改 `now.json` / `sampling.json` 同理，它们更短，改起来更快。

## 新手记模板（复制粘贴后改内容）

```json
{
  "id": "no-04",
  "no": "NO.04",
  "tag": "随手记",
  "title": "标题写这里",
  "highlight": "整篇最重要的一句话，会被红笔画线。",
  "paragraphs": [
    "第一段。",
    "第二段。",
    "第三段。"
  ],
  "footnote": "相关：作品页 · 案卷 XX《xxx》"
}
```

注意：
- `id` 和 `no` 的编号往上加（no-04、no-05…）
- 新的一篇加在**数组末尾**（最后一个 `}` 后面加 `,` 再接新 `{`）
- `paragraphs` 想写几段写几段
- 全部标点用**英文引号** `"..."`，文内的书名号《》没问题

## 新采样笔记模板

```json
{
  "track": "歌名《曲目》",
  "chain": "← 原曲《名字》 · 年份",
  "root": null,
  "note": "一句话：这个采样妙在哪。"
}
```

（如果原曲本身也是采样来的，`root` 填 `← 更早的原曲 · 年份`，否则保持 `null`）

## 「此刻」条

```json
[
  { "label": "正在听", "value": "……" },
  { "label": "正在做", "value": "……" },
  { "label": "正在找", "value": "……" }
]
```

三项固定，改 `value` 里的文字即可。

## JSON 语法三条铁律（改错会构建失败）

1. 字符串用**英文双引号** `"…"` 包住，文内的中文引号「」《》随意
2. 每条数据之间用**逗号**隔开，最后一条**不加**逗号
3. `null` / `true` / `false` / 数字 不加引号

改错了也不要慌：GitHub 会显示红色 ❌（Actions 构建失败），网站保持旧版不受影响，
回去把文件改对再提交一次就好。

## 长文建议：先在备忘录写好

手机小屏改长文容易丢内容。推荐流程：
备忘录/微信文件传输助手写好 → 复制 → GitHub 网页粘贴 → 提交。

## 用电脑时（可选）

```bash
git pull                          # 先拉最新
# 改 src/data/content/*.json
git add -A
git commit -m "手记：新增《xxx》"
git push                          # push 即自动部署
```

（本地 `bash scripts/deploy-ghpages.sh` 仍然可用，但一般不再需要）
